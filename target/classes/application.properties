spring.application.name=sprint1

# --------------------------------------------
# Configura��o do Banco Oracle
# --------------------------------------------
# Configura��es do Oracle
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/orcl
spring.datasource.username=rm558981
spring.datasource.password=fiap
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --------------------------------------------
# Configura��o de JPA
# --------------------------------------------
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# --------------------------------------------
# Configura��o de Cache
# Utiizando implementacao padrao do spring, sem complexidade. Utilizada para testes
# --------------------------------------------
spring.cache.type=simple

# --------------------------------------------
# Ativa Logs B�sicos do Cache
# --------------------------------------------
logging.level.org.springframework.cache=DEBUG




# editar a funcionario gerente e motos

